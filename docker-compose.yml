version: '3.8'
services:
  intel-monitor:
    build: .
    container_name: intel-monitor
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config.json:/app/config.json:ro
    environment:
      - TZ=Asia/Kolkata
